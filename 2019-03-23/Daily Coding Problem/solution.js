/**
 * Job Scheduler function
 * Not sure how this should be implemented, so I'll do what I think it does
 * @params function, integer/number
 * @returns function
 */

 const solutionA = (f, n) => {
    setTimeout(f,n);
 }